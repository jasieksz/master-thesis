\chapter{Voter/Candidate Range Profiles}\label{ch:domain}

\ifpdf
    \graphicspath{{Chapter4/Figs/Raster/}{Chapter4/Figs/PDF/}{Chapter4/Figs/}}
\else
    \graphicspath{{Chapter4/Figs/Vector/}{Chapter4/Figs/}}
\fi

In this section we explore the Voter/Candidate Range domain, describe some of its properties,
and compare it with the Voter Range and Candidate Range ones.
We already know that the VCR domain is larger than the VR and CR ones combined (Section \ref{VCR-definition}, Example \ref{ch:pre:ex:tvcr}).
However, we would like to know how much bigger it actually is,
and if we can significantly benefit from the further development of algorithms based on the VCR model. 

\section{Domain analysis}

We have performed several experiments, in each of them we check how many profiles have
the VCR property but not the VR or CR ones. 
For the first one we focus on the small size elections, and 
for the second experiment we employ several strategies to generate random, larger profiles.
In every experiment we use the detection algorithm described in Section \ref{ch:detection:ilp}.

\section{How many VCR profiles are not VR neither CR?}

\begin{definition}
    A profile P is \textbf{Truly VCR (TVCR)}, only if P satisfies the VCR property and does not satisfy the VR and CR properties.
\end{definition}

\begin{definition}
    A profile P is \textbf{Fully COP (FCOP)}, only if P satisfies the VR and CR properties.
\end{definition}

\subsection{Experiment 1 — Small Profiles}

For the small profiles we performed a complete search of the domain.
That is we generated every possible profile of a given size and checked if it has the VCR property.
We checked 6 different dimensions of the elections, having $M$ candidates and $N$ voters,
we used the following, $(M,N) \in \{(3,3), (4,4), (5,5), (4,6), (6,4)\}$.
The results, presented in Figure \ref{fig:small-statistics-barchart} show that
the VCR domain is relatively large. However, for these small election sizes, only a limited number of profiles
are Truly VCR (i.e., neither VR nor CR).

\begin{figure}[H]
    \includegraphics[width=\linewidth]{small-statistics-barchart.png}
    \centering
    \caption{Temporary Image - Aggregated Statistics For Small Profiles}
    \label{fig:small-statistics-barchart}
\end{figure}

\subsection{Experiment 2 — Large Profiles}

For the larger size of the profiles, we generated a suitable number of random VCR profiles
and again checked how many of them are neither CR nor VR (\textit{TVCR}). 
We grouped the results into three additional categories, besides the TVCR one,
that is, VR and CR (\textit{FCOP}), VR and not CR, CR and not VR.

We employ several strategies to generate a large number of profiles with distinct characteristic.
We use various statistical cultures (distributions) to pick random points and radii representing the candidates and voters.
Furthermore, we choose the random distribution independently for the position and radius parameters,
and independently as well for the candidates and voters.
We describe these categories in detail below.
For each category pair (in total 20 pairs) we generate 1000 elections with 20 candidates and 20 voters,
then we repeat the experiment for the elections consisting of 40 candidates and 40 voters.
 

\subsubsection{Position Parameter}
We consider 4 ways of generating the points, representing the positions of the candidates and the voters.

\begin{enumerate}
    \item The \textit{Uniform Candidates, Uniform Voters} model,
    where the points for both, the candidates and the voters are selected uniformly at random from the $[-10,10]$ range.
    
    \item The \textit{Asymmetric Gauss Candidates, Asymmetric Gauss Voters} model,
    where we take 70\% of points from a Gaussian distribution with mean value $-3$ and standard deviation $1.8$,
    and the remaining 30\% come from a Gaussian with mean $3$ and the same standard deviation. 
    This model is inspired by the 2 dimensional model from the work of \cite{faliszewski-vcr}. 
    The goal is to simulate the society with a large majority, and a minority with the opposite opinion. 

    \item The \textit{Uniform Candidates, Asymmetric Gauss Voters} model,
    it is a combination of the previous two models, where
    the candidate's points are selected uniformly at random,
    and for the voters we use asymmetric Gaussian model.
    We use the same parameters for both of the distributions.

    \item The \textit{Asymmetric Gauss Candidates, Uniform Voters} model,
    it is mirror version of the previous model, with the distributions swapped.

\end{enumerate}

\subsubsection{Radius Parameter}
In this category we consider 5 ways of generating the radii.
We split the methods between the candidates and the voters, similarly to picking the positions.

\begin{enumerate}
    \item\label{enum:radius-gaus-gaus} The \textit{Gauss Candidates, Gauss Voters} model, 
    where we choose the radii from the Gaussian distribution with mean value $1.5$ and standard deviation $0.5$
    We use the same distribution for the candidates and the voters.

    \item The \textit{Small Uniform Candidates, Gauss Voters} model.
    We pick radii for the candidates uniformly at random from the range $[0.7,1.2]$,
    and for the voters we use the same Gaussian, as in the previous model ($Gauss(1.5, 0.5)$).

    \item The \textit{Gauss Candidates, Small Uniform Voters} model.
    In this model we swap the distributions used in the previous one,
    that is, the candidates are picked from the Gaussian and the voters from the uniform distributions.

    \item\label{enum:suc-suv} The \textit{Small Uniform Candidates, Small Uniform Voters} model.
    We pick radii for both the candidates and the voters, uniformly at random from the range $[0.7,1.2]$.

    \item The \textit{Large Uniform Candidates, Large Uniform Voters} model.
    We pick radii for both the candidates and the voters, uniformly at random from the range $[0,3]$.

\end{enumerate}

At first, it might be surprising why we do not include a constant radius, as one of the distributions.
That is because, then, all the profiles would be VR and CR (\textit{FCOP}).
Below in Proposition \ref{prop:vcr-constR-vrcr}, we show why.

\begin{proposition}\label{prop:vcr-constR-vrcr}
    If every agent (candidate/voter) in a VCR profile, has the same constant radius, then
    this profile satisfies both the VR and CR properties (FCOP). 
    \begin{proof}
        Consider a VCR election $E = (C,V)$, where $C = \{c_1,...,c_M\}$ and $V = \{v_1,...,v_N\}$,
        and a constant radius $R$. An agent $a \in C \cup V$ is represented by a point $x_a$ and a radius $r_a$,
        now every agent has the same radius $r_a = R$.
    \end{proof}
\end{proposition}

\subsubsection{Results}
First, we present the visualization of our example election profiles,
and how their visual structure is impacted by the generation parameters in Figure \ref{fig:20-20-examples-grid}.
Later we analyze aggregated results of the experiment.
Most of the conclusions we draw are inline with the basic intuition.
\newpage

\begin{figure}[H]
    \includegraphics[width=\linewidth]{40-40-examples-grid.png}
    \centering
    \caption{40C 40V profile examples — approval matrix heatmaps}
    \label{fig:20-20-examples-grid}
\end{figure}

\noindent \textbf{Election Dimensions} 
\indent Our results for two election sizes have one crucial difference,
looking briefly at Figure \ref{fig:20-20-statistics-barchart} (20 candidates, 20 voters)
and Figure \ref{fig:40-40-statistics-barchart} (40 candidates, 40 voters), 
we immediately see, that in the larger elections \textit{TVCR} property is much more frequent.
\textcolor{orange}{(WIP: Napisać jakąś hipotezę, dlaczego tak się dzieje?)}
Election sizes in the experiment are relatively small when compared to the real world scenarios.
We can expect for this trend of increased number of \textit{TVCR} profiles to continue for even larger elections.
This result is very promising, as it strongly encourages further development and research related to the VCR domain.
\newline 

\begin{figure}[H]
    \includegraphics[width=\linewidth]{20-20-statistics-barchart.png}
    \centering
    \caption{Temporary Image - 20C 20V Aggregated Statistics}
    \label{fig:20-20-statistics-barchart}
\end{figure}

\begin{figure}[H]
    \includegraphics[width=\linewidth]{40-40-statistics-barchart.png}
    \centering
    \caption{Temporary Image - 40C 40V Aggregated Statistics}
    \label{fig:40-40-statistics-barchart}
\end{figure}

In the subsequent paragraphs we will focus mostly on the results for smaller elections from
Figure \ref{fig:20-20-statistics-barchart}, but only because the differences between the parameters are more pronounced.
\newline

\begin{figure}[H]
    \includegraphics[width=\linewidth]{20-20-statistics-barchart-GG.png}
    \centering
    \caption{Temporary Image - Selected Histogram - Double Gauss Radius Aggregated Statistics}
    \label{fig:20-20-GG-barchart}
\end{figure}

\noindent \textbf{Position Choice} \indent We analyze
Figure \ref{fig:20-20-GG-barchart} (subgraph from Figure \ref{fig:20-20-statistics-barchart}) — histogram 
for the \textbf{\textit{Gauss Candidates, Gauss Voters}} radius model.
Because this model exhibits the most neutral behavior, we consider it as our baseline scenario.
We can compare the position models in pairs. 
Starting with the \textit{Uniform Candidates, Uniform Voters} and \textit{Asymmetric Gauss Candidates, Asymmetric Gauss Voters} models.
The latter one has noticeably more \textit{TVCR} profiles,
it can be easily explained by looking at the respective heatmaps of the approval matrices in Figure \ref{fig:20-20-examples-grid}.
The profiles generated from the double Gauss model have higher count of approvals,
which in turn increases the likelihood of any irregularities in the approvals block structure,
which we directly link to the \textit{TVCR} property.   
\textcolor{orange}{(WIP: lepiej wyjaśnić, mam na myśli "dziury" w blokach jedynek w macierzy)}

Now turning our attention to the second pair of the position models (still for the same radius model),
\textit{Uniform Candidates, Asymmetric Gauss Voters} and \textit{Asymmetric Gauss Candidates, Uniform Voters}.
These are two perfectly symmetrical models, we explain only the first one, and keep in mind that the second one is simply a reversed scenario.
Looking at the heatmaps again, we notice that the blocks of approvals are skewed horizontally,
which is associated with the VR property, i.e., the ones form and interval in each row of the approval matrix.
On the contrary, in the second model, the blocks are skewed vertically, which links to the CR property.
\newline

\noindent \textbf{Radius Choice} \indent From Figure \ref{fig:20-20-statistics-barchart} it is clear that the choice of radius
can significantly reinforce or subside trends (in the profiles properties statistics) resulting from the position choice.
For example, the mixed radius models, i.e., \textit{Small Uniform Candidates, Gauss Voters} and \textit{Gauss Candidates, Small Uniform Voters}.
In particular, these models increase occurrence of profiles with the VR or CR properties.
The core idea behind this phenomenon is exactly the same as the one described above, for the mixed position models (Uniform, Gauss),
where the blocks of approvals are skewed, either horizontally or vertically, resulting in the increased number of the VR or CR profiles respectively.

The striking difference between histograms for
\textit{Small Uniform Candidates, Small Uniform Voters} and \textit{Large Uniform Candidates, Large Uniform Voters}
is again related to the inconsistencies in the approval blocks, which are easily observed in the heatmaps. 
Smaller radius range produces much more structured profiles, hence large number of the \textit{FCOP} profiles.
In contrast, large radius range outputs irregular approval blocks in the profiles, increasing the number of the \textit{TVCR} profiles.
\newline

\noindent \textbf{Summary} \indent
The results are very promising,
we showed that the \textit{VCR} domain can be significantly larger than the \textit{VR} and \textit{CR} combined.
We hope, this will encourage further development and research related to the VCR domain.

\textcolor{orange}{(WIP: Więcej/Lepsze wnioski? - to jest powtórzenie z końcówki subsekcji Election Dimensions)}

\section{Transforming a Truly VCR Profile into VR or CR one}
A natural question that arises from the previous experiment results, 
is how different are the TVCR profiles from the VR or CR ones? 
In other words, how much do we need to change a TVCR profile for it to become a VR or CR one. 
To answer this question, we design yet another experiment.

\subsection{Experiment 3 — Profile Transformation}

In this experiment, we partially reuse the strategy for profile generation from the previous one.
We narrow down our experiment parameters to only two radius generation methods,
that is, first model \textit{Gauss Candidates, Gauss Voters} and second \textit{Large Uniform Candidates, Large Uniform Voters}.
However, we keep the position models unchanged, and explore all four of them.
Obviously we only pick the TVCR profiles, to be exact, we use 400 of them for each category pair (in total 8 pairs).
Due to the limited computational resources, we only use the profiles consisting of 15 candidates and 15 voters.
\newline

\noindent \textbf{Agent Deletion Parameter} \indent We consider 3 methods to transform a TVCR profile into a VR or CR one.
\begin{enumerate}
    \item The \textit{Candidate Deletion} method, 
    where we search for a minimal number $d$,
    such that, after deleting $d$ candidates from the profile it becomes a VR or CR profile.

    \item The \textit{Voter Deletion} method, 
    which is similar to the \textit{Candidate Deletion} method, but this time we delete $d$ voters instead.

    \item \sout{The \textit{Candidate and Voter Deletion} method, 
    where we combine the two previous methods, and we simultaneously delete $d_c$ candidates and $d_v$ voters.}
    \textcolor{orange}{(WIP: Ciagle sie liczy, większość wyników to wystarczy usunąć 1 C i 1 V)}
\end{enumerate}


\noindent \textbf{Transformation Result Parameter} \indent Apart from choosing what type of agent (candidate / voter) to delete,
for each profile we collect the transformation results into 3 groups.
By the transformation result we mean the lowest possible cost of the transformation,
i.e., the minimal number of agent deletions for a TVCR profile to become a VR or CR one.
\begin{enumerate}
    \item The \textit{Candidate Range}, where the cost is a number of deletions required to get a CR profile.
    \item The \textit{Voter Range}, where the cost is a number of deletions required to get a VR profile.
    \item The \textit{Candidate Range or Voter Range}, where the cost is a minimum from the previous two groups. 
\end{enumerate}

\subsubsection{Results}
This experiment further confirms, our basic conclusions from the previous one.
We use the same ideas and concepts, as before, to explain the results.
Looking at Figure \ref{fig:transform-grid1}, we can distinguish two groups.
First, the charts in the left half of the figure,
they look somewhat more balanced, or  one may say symmetrical.
These are the results for the 
\textit{Gauss Candidates, Gauss Voters} and \textit{Uniform Candidates, Uniform Voters} position models.
Looking back at the result of the previous experiment for these models in Figure \ref{fig:20-20-GG-barchart},
we see that, the numbers of the CR and VR profiles were equal. This tells us, that all the other profiles (TVCR ones),
were similarly different from the CR or VR ones, hence we don't see much difference between the visualizations (violins).


\begin{figure}[H]
    \includegraphics[width=\linewidth]{15-15-tr-grid-xprop-coldist-rowR.png}
    \centering
    \caption{XAxis = property | COL = point| ROW = radius \newline
    Each violin represents the distribution of required number of agent deletions, for given parameters}
    \label{fig:transform-grid1}
\end{figure}

\begin{figure}[H]
    \includegraphics[width=\linewidth]{15-15-tr-grid-xdist-colprop-rowR.png}
    \centering
    \caption{XAxis = point | COL = property | ROW = radius}
    \label{fig:transform-grid2}
\end{figure}

\begin{figure}[H]
    \includegraphics[height=\textheight]{15-15-tr-grid-xdist-colR-rowprop.png}
    \centering
    \caption{XAxis = point | COL = radius | ROW = property}
    \label{fig:transform-grid3}
\end{figure}

\begin{figure}[H]
    \includegraphics[height=\textheight]{15-15-tr-grid-xprop-colR-rowdist.png}
    \centering
    \caption{XAxis = property | COL = radius | ROW = point}
    \label{fig:transform-grid4}
\end{figure}
