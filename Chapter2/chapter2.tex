\chapter{Preliminaries}

% **************************** Define Graphics Path **************************
\ifpdf
    \graphicspath{{Chapter2/Figs/Raster/}{Chapter2/Figs/PDF/}{Chapter2/Figs/}}
\else
    \graphicspath{{Chapter2/Figs/Vector/}{Chapter2/Figs/}}
\fi

First, in Section \ref{sec:basic-def}, we introduce basic definitions and core concepts, together with simple examples.
Later, in Section \ref{sec:pref-restrictions}, we discuss preference restrictions,
and how can we leverage them to design better algorithms, for many of the computational social choice problems.
Finally, in Section \ref{sec:methods}, we explain some computational methods (tricks) we have used in this work,
that we believe are not necessarily straightforward. 

\section{Basic Definitions}\label{sec:basic-def}

\subsubsection{Approval Elections}

An approval-based election (in short, an \textit{election}, or interchangeably a \textit{profile}) is a pair $E = (C, V)$, where 
$C = \{c_1,...,c_M\}$ is a set of candidates and  $V = \{v_1,...,v_N\}$ is a set of voters.
We use $M$ and $N$ for the number of candidates and voters respectively. 
For each voter $v \in V$, his or hers preferences are represented as a set of approved candidates $A(v)$,
we call it an \textit{approval set}.
Furthermore, for each candidate $c \in C$, we define $V(c) = \{v \in V \:|\: c \in A(v)\}$,
a set of voters who approve candidate $c$.

In practice, we often use an additional, simplified notation of representing an election.
We redefine $A$ as a two-dimensional binary matrix, with $N$ rows and $M$ columns,
where value of $A[i][j]$ is $1$ if i-th voter approves of j-th candidate, and $0$ otherwise.
In other words, each row of the matrix is a vector representation of a given voter's approval set.
We refer to it as an \textit{approval matrix} or a \textit{preference matrix}.
It will always be clear from the context which notation we are using.

\begin{figure}[H]
    \includegraphics[width=\linewidth]{approval-election.png}
    \centering
    \caption{Temporary Image - Example of An Approval Election \cite{lacknerSkowronAVOverview}}
    \label{fig:approval-election-example}
\end{figure}

\begin{example}\label{ex:approval-election}
    \textcolor{orange}{(WIP: Opis wyborów + figura)}    
    Following the example from Figure \ref{fig:approval-election-example} ...
\end{example}

\subsubsection{Multiwinner Committee Selection Rules}

Voting rules are algorithms used to, select a committee (winners) — fixed size subset of the candidates,
based on the voters preferences, for a given election.
Each rule has different properties and must be selected according to the goal of the election.
As this is not the main focus of this work we point to \cite{faliszewskiSkowronMultiWinnerOverview},
for a detailed overview of multiwinner voting rules.

Nonetheless, we introduce the most basic voting rule for the approval model, simply called \textit{Approval Voting (AV)},
introduced by \cite{brams1983approval}.

\begin{definition}[Multi-Winner Approval Voting (AV)]
    For a given election $E$ and a size of committee $k$, AV rule selects the $k$ candidates with the highest approval scores.
    AV-score of a given candidate $c$, is the number of voters who approve of $c$, and 
    formally, is defined as $score_{AV}(E,c) = |\{v \in V : c \in A(v)\}|$.
\end{definition}

\begin{example}
    \textcolor{orange}{(WIP: Opis wyborów z poprzedniego przykładu + kto wygra)}
    Continuing with the election from Example \ref{ex:approval-election}
\end{example}

\section{Preference Restrictions}\label{sec:pref-restrictions}
As a reminder from Section \ref{ch:intro:sec:restrictions},
many computational social choice problems such as winner determination, or control problems are known to be computationally hard.
Preference restrictions, i.e., restrictions on the structure of voters’ ballots,
is a very effective solution to circumvent the computational complexity of these problems.

\subsection{Euclidean Preference Restrictions}
Euclidean preference restrictions, in the approval voting model, are foundation of this work.
All the problems we solve in the subsequent chapters are structured around this concept.
Unless stated otherwise, throughout this work, we consider one-dimensional variants of the Euclidean models.

Let us introduce restrictions that we explore in this work.
Starting with the most basic one, that is a predecessor to the other ones,
in this family of restrictions. 

\begin{pref-restriction}[Dichotomous Euclidean (DE)][\cite{elkind-approval-restrictions}]

Given an election $E = (C,V)$, we say that $E$ satisfies DE
if there is a mapping $\rho$ of voters and candidates into the real line such that
for every voter $v \in V$ there exists a radius $r_v$, and it holds that $v = \{c : |\rho(v) - \rho(c)| \leq r_v\}$.
\textcolor{orange}{(WIP: Czy w ogóle definiować DE?, czy jest potrzebna cytacja?, ta definicja
nie jest specjalnie spójna z pozostałymi oznaczeniami / nie oczywista)}   
\end{pref-restriction}

\begin{pref-restriction}[Voter/Candidate Range (VCR)][\cite{faliszewski-vcr}]\label{ch2:def:vcr}
Given an election \(E = (C, V)\), we say that the voters have one-dimensional Euclidean preferences if
for each agent $a \in C \cup V$ (i.e., for each candidate and each voter)
there exists a point $x_a \in \mathbb{R}$ and nonnegative real value $r_a \in \mathbb{R}$ such that:
\begin{displaymath}
    c \in A(v) \Longleftrightarrow |x_c - x_v| \leq r_c + r_v
\end{displaymath}
\end{pref-restriction}

We can easily think of the VCR restriction as,
for an agent $a \in C \cup V$ the point $x_a$ describes $a$'s ideal position 
in $\mathbb{R}$, representing his or hers opinion. 
For a candidate $c \in C$, radius $r_c$ can be seen as $c$'s charisma,
or in other words their ability to reach a number of different voters
(e.g., a centrist candidate would have a larger reach, than an extremist one)
For a voter $v \in V$, radius $r_v$ corresponds to $v$'s willingness to compromise,
that is, how different (faraway) opinion they would still accept from their ideal one.
Moreover, there are two special cases of the VCR model.
\begin{enumerate}
    \item \textit{Candidate Range (CR)}, we say that a VCR election $E = (C,V)$, 
    is CR if for each voter $v \in V$, their radius $r_v$ is equal to zero. 
    
    \item \textit{Voter Range (VR)}, we say that a VCR election $E = (C,V)$, 
    is VR if for each candidate $c \in C$, their radius $r_c$ is equal to zero. 
\end{enumerate}

The CR and the VR models are equivalent to the voter interval (VI) and the candidate interval (CI) ones,
both introduced by \cite{elkind-approval-restrictions}. \cite{faliszewski-vcr} simply use a different terminology.

\begin{figure}[h]
    \includegraphics[width=8cm]{vr-cr.png}
    \centering
    \caption{CR and VR Temporary image from \cite{cr-vr-mav-liu-guo}}
\end{figure}

The VCR domain is larger than the CR and VR domains combined.
Below in Example \ref{ch:pre:ex:tvcr}, we show an election that is VCR, but neither CR nor VR.
We explore this topic further in Chapter \ref{ch:domain}.

\begin{example}\label{ch:pre:ex:tvcr}
Given an election $E$ with the candidates $C = \{a, b, c, d\}$ and 
the voter's approval sets $\{a, b\}$, $\{b, c\}$, $\{b, d\}$, and $\{a, b, c, d\}$.

To see why, ... 
\textcolor{orange}{(WIP: Podać punkty i promienie (VCR), następnie opisać dlaczego nie jest CR ani VR ... Obrazek?)}   
\end{example}

\section{\textcolor{orange}{WIP: Methodology/Techniques}}\label{sec:methods}

\subsection{Integer Linear Program}
Integer linear programming (ILP) is a common approach to solve NP-hard problems, 
furthermore it can be used effectively for other problems as well.
Formally, a linear program is a system of linear inequalities and an objective function.

To define a computational problem as an integer linear program,
we have to create a system of linear inequalities (constraints) 
and 
optionally a linear function to optimize, alternatively we can just check feasibility of the constraints.
Next to obtain a solution we have to use an optimization solver.
Popular choices are Gurobi or CPLEX which are state-of-the-art general purpose solvers.