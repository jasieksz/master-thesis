%!TEX root = ../thesis.tex
%*******************************************************************************
%****************************** Second Chapter *********************************
%*******************************************************************************

\chapter{Approval-Based Elections in the \textit{1D} Euclidean Domain}

\ifpdf
    \graphicspath{{Chapter3/Figs/Raster/}{Chapter3/Figs/PDF/}{Chapter3/Figs/}}
\else
    \graphicspath{{Chapter3/Figs/Vector/}{Chapter3/Figs/}}
\fi

\section{Euclidean Preference Restrictions}

\subsubsection{Voter Candidate Range}
\indent
Given an election \(E = (C, V)\), we say that the voters have \textit{1D-Euclidean} preferences 
For each agent there exists a point $x_a$ and nonnegative real value $r_a$ such that:
$c \in A(v) \Leftrightarrow \sqrt{(x_c - x_v)^2} \leq r_c + r_v$

Intuitively, for $a \in C \cup V$ the point $x_a$ describes a's ideal position 
in 1-dimensional space of opinions.
For candidate $c \in C, r_c$ can be seen as c's charisma.
For candidate $v \in V, r_v$ can be seen as v's willingness to compromise. \cite{falisz}

\subsubsection{Candidate Range}
\indent
We say that P satisfies CR if candidates in C can be
ordered so that each of the sets vi forms an interval of that ordering.
CR is special case of VCR, where we require all the voters to have radii equal to zero

\subsubsection{Voter Range}
\indent
We say that P satisfies VR if the voters in P can be reordered
so that for every candidate c the voters that approve c form an interval of that
ordering.
VR is a special case of VCR, where we require all the candidates to have radii equal to zero

\begin{figure}[h]
    \includegraphics[width=8cm]{vr-cr.png}
    \centering
    \caption{VR and CR Temporary image}
\end{figure}

\cite{falisz} showed that \textbf{CR} and \textbf{VR} are equivalent to respectively \textbf{VI} and \textbf{CI},
introduced by \cite{DBLP:journals/corr/ElkindL15}.

\subsubsection{Larger Domain}

The VCR model is strictly more powerful than the VR and CR ones.
For example, election with candidate set
$C = \{a, b, c, d\}$ and voters with approval sets $\{a, b\}$, $\{b, c\}$,
$\{b, d\}$, and $\{a, b, c, d\}$ is VCR, but neither VR nor CR.


\subsection{VR and CR Detection Algorithms}
Profile is VR or CR if the matrix or any permutation has consecutive ones property, that is for each row (VR) or column (CR),
entries with 1 are in the uninterrupted sequence.

The fastest known algorithm for detection of C1P is based on the \textit{PQ-tree} algorithms by \cite{COP-PQTree}.
However in this work I'm using still relatively fast and much easier to implement approach based on the \textit{Lex-BFS} algorithm by \cite{COP-LexBFS}

( WIP ) Change of the solution, using ILP instead - described in the ch.4 

\subsection{VCR Detection Algorithms}
( WIP ) change titles of subsections and merge them together?

Voter Candidate Range detection algorithms are described in detail in the subsequent chapters.

\newpage
\section{(WIP) Committee Scoring Rules - Motivation}
Explain how VR/CR/VCR can change complexity of computing rules, detection, etc.

Section 4.1 in \cite{falisz}

"Elkind and Lackner (2015) have shown polynomial-time algorithms for computing CC 
winning committees in 1D-VR and 1D-CR elections.
We unify this result into a single algorithm for 1D-VCR elections (the main idea is remarkably close to that for the 1D-VR case).
Proposition 2. There is an algorithm that given a 1D-VCR election  and  committee  size, 
computes  some  winning  $C$ committee in polynomial time.
A natural question is whether it is possible to extend the above  result  to  PAV  and  other  Thiele  methods.
While  we leave this question open, in Appendix B we explain that the existing technique that allowed to 
construct a polynomial-time algorithm  for  a  large  class  of  Thiele  rules  under  the 1D-VR model (Peters and Lackner 2020)
cannot be easily applied to the 1D-VCR setting. 
On the other hand, it may be possible to obtain a 1D-VCR algorithm for MAV as 1D-VR and 1D-CR algorithms do exist (Liu and Guo 2016)."