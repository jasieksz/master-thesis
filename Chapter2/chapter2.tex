\chapter{Preliminaries}

% **************************** Define Graphics Path **************************
\ifpdf
    \graphicspath{{Chapter2/Figs/Raster/}{Chapter2/Figs/PDF/}{Chapter2/Figs/}}
\else
    \graphicspath{{Chapter2/Figs/Vector/}{Chapter2/Figs/}}
\fi

First, in Section \ref{sec:basic-def}, we introduce basic definitions and core concepts, together with simple examples.
Later, in Section \ref{ch2:sec:pref-restrictions}, we discuss preference restrictions,
and how can we leverage them to design better algorithms, for many of the computational social choice problems.
Finally, in Section \ref{{ch2:sec:methods}}, we explain some computational methods (tricks) we have used in this work,
that we believe are not necessarily straightforward. 

% =====================
% Basic Definitions
% =====================
\section{Basic Definitions}\label{sec:basic-def}

For an integer $t$, by $[t]$ we mean the set $\{1,...,t\}$.
To describe a matrix $\mathbf{A}$ with $r$ rows and $c$ columns, we use $\mathbf{A}_{r \times c}$.
By $\mathbf{A}[i,j]$ we refer to the entry in i-th row and in j-th column of a matrix $\mathbf{A}$.

\subsubsection{Approval Elections}

An approval-based election (in short, an \textit{election}, or interchangeably a \textit{profile}) is a pair $E = (C, V)$, where 
$C = \{c_1,...,c_M\}$ is a set of candidates and  $V = \{v_1,...,v_N\}$ is a set of voters.
We use $M$ and $N$ for the number of candidates and voters respectively. 
For each voter $v \in V$, his or hers preferences are represented as a set of approved candidates $A(v)$,
we call it an \textit{approval set}.
Furthermore, for each candidate $c \in C$, we define $V(c) = \{v \in V \:|\: c \in A(v)\}$,
a set of voters who approve candidate $c$.

In practice, we often use an additional, simplified notation of representing an election.
We redefine $A$ as a binary matrix (a matrix, where each entry is in $\{0,1\}$), with $N$ rows and $M$ columns.
For $i \in [N]$ and $j \in [M]$, entry $A[i,j]$ is $1$ if voter $v_i$ approves of candidate $c_j$, and $0$ otherwise.
In other words, each row of the matrix is a vector representation of a given voter's approval set.
We refer to it as an \textit{approval matrix} or a \textit{preference matrix}.
It will always be clear from the context which notation we are using.

\begin{figure}[H]
    \includegraphics[width=\linewidth]{approval-election.png}
    \centering
    \caption{Example of an approval election.
    A candidate is approved by a voter if his or hers block is above the voter's point. 
    Temporary image from \cite{lacknerSkowronAVOverview}}
    \label{fig:approval-election-example}
\end{figure}

\begin{example}\label{ex:approval-election}
    \textcolor{orange}{(WIP: Opis wyborów z figury)}    
    Following the example from Figure \ref{fig:approval-election-example} ...
\end{example}

\subsubsection{Multiwinner Committee Selection Rules}

Voting rules are algorithms used to, select a committee (winners) — fixed size subset of the candidates,
based on the voters preferences, for a given election.
Each rule has different properties and must be selected according to the goal of the election.
As this is not the main focus of this work we point to \cite{faliszewskiSkowronMultiWinnerOverview},
for a detailed overview of multiwinner voting rules.

Nonetheless, we introduce the most basic voting rule for the approval model, simply called \textit{Approval Voting (AV)},
introduced by \cite{brams1983approval}.

\begin{definition}[Multi-Winner Approval Voting (AV)]
    For a given election $E = (C, V)$ and a size of committee $k$, AV rule selects the $k$ candidates with the highest approval scores.
    AV-score of a given candidate $c$, is the number of voters who approve of $c$, and 
    formally, is defined as $score_{AV}(E,c) = |\{v \in V : c \in A(v)\}|$.
\end{definition}

\begin{example}
    \textcolor{orange}{(WIP: Opis wyborów z poprzedniego przykładu + kto wygra)}
    Continuing with the election from Example \ref{ex:approval-election}
\end{example}

% =====================
% Preference Restrictions
% =====================

\section{Preference Restrictions}\label{ch2:sec:pref-restrictions}
As a reminder from Section \ref{ch:intro:sec:restrictions},
many computational social choice problems such as winner determination, or control problems are known to be computationally hard.
Preference restrictions, i.e., restrictions on the structure of voters’ ballots,
are a very effective solution to circumvent the computational complexity of these problems.

\subsection{Euclidean Preference Restrictions}
Euclidean preference restrictions, in the approval voting model, are foundation of this work.
All the problems we solve in the subsequent chapters are structured around this concept.
Unless stated otherwise, throughout this work, we consider one-dimensional variants of the Euclidean models.

Let us introduce restrictions that we explore in this work.
Starting with the most basic one, that is a predecessor to the other ones,
in this family of restrictions. 

\begin{pref-restriction}[Dichotomous Euclidean (DE)][\cite{elkind-approval-restrictions}]

Given an election $E = (C,V)$, we say that $E$ satisfies DE
if there is a mapping $\rho$ of voters and candidates into the real line such that
for every voter $v \in V$ there exists a radius $r_v$, and it holds that $v = \{c : |\rho(v) - \rho(c)| \leq r_v\}$.
\textcolor{orange}{(WIP: Czy w ogóle definiować DE?, czy jest potrzebna cytacja?, ta definicja
nie jest specjalnie spójna z pozostałymi oznaczeniami / nie oczywista)}   

\end{pref-restriction}

The DE preference restriction is somewhat limited, it permits for only one agent type, i.e., candidate or voter,
to have a radius parameter, while the other one is represented just by his or hers point.
We consider the DE model in one of the two variants:
\begin{enumerate}
    \item The voter-range model, where voters have the radius parameter.
    In other words, if a voter approves a candidate then he or she also approves all the closer ones.

    \item The candidate-range model, where candidates have the radius parameter.
    We say that if a candidate is approved by a voter, then he or she is also approved
    by all the voters closer to him or her.
\end{enumerate}
Below we provide a formal definition of these two models,
as well as their generalization which allows all agents to have a radius parameter.

\begin{pref-restriction}[Voter/Candidate Range (VCR)][\cite{faliszewski-vcr}]\label{ch2:def:vcr}

Given an election \(E = (C, V)\), we say that the voters have one-dimensional Euclidean preferences if
for each agent $a \in C \cup V$ (i.e., for each candidate and each voter)
there exists a point $x_a \in \mathbb{R}$ and nonnegative real value $r_a \in \mathbb{R}$ such that:
\begin{displaymath}
    c \in A(v) \Longleftrightarrow |x_c - x_v| \leq r_c + r_v
\end{displaymath}

\end{pref-restriction}

We can think of the VCR restriction as
for an agent $a \in C \cup V$ the point $x_a$ describes $a$'s ideal position 
in $\mathbb{R}$, representing his or hers opinion. 
For a candidate $c \in C$, radius $r_c$ can be seen as $c$'s charisma,
or in other words their ability to reach a number of different voters
(e.g., a centrist candidate possibly would have a larger reach, than an extremist one).
For a voter $v \in V$, radius $r_v$ corresponds to $v$'s willingness to compromise,
that is, how different (faraway) opinion they would still accept from their ideal one.
Moreover, there are two special cases of the VCR model.

\begin{pref-restriction}[Candidate Range (CR)]
We say that a VCR election $E = (C,V)$, 
is CR if for each voter $v \in V$, their radius $r_v$ is equal to zero. 
\end{pref-restriction}

\begin{pref-restriction}[Voter Range (VR)]
We say that a VCR election $E = (C,V)$, 
is VR if for each candidate $c \in C$, their radius $r_c$ is equal to zero. 
\end{pref-restriction}

The CR and the VR models are equivalent to the voter interval (VI) and the candidate interval (CI) ones,
both introduced by \cite{elkind-approval-restrictions}. \cite{faliszewski-vcr} simply use a different terminology.

\begin{figure}[H]
    \begin{subfigure}{0.49\textwidth}
      \centering
      \includegraphics[width=\linewidth]{cr-example.png}
      \caption{CR election}
      \label{fig:cr-example}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.49\textwidth}
      \centering
      \includegraphics[width=\linewidth]{vr-example.png}
      \caption{VR election}
      \label{fig:vr-example}
    \end{subfigure}
    \caption{Example of elections with the CR and the VR preference restrictions.
    Temporary Image from \cite{elkind-approval-restrictions}
    }
\end{figure}

The VCR domain is larger than the CR and the VR domains combined.
Below in Example \ref{ch:pre:ex:tvcr}, we show an election that is VCR, but neither CR nor VR.
We explore this topic further in Chapter \ref{ch:domain}.

\begin{example}\label{ch:pre:ex:tvcr}
Given an election $E$ with the candidates $C = \{a, b, c, d\}$ and 
the voter's approval sets $\{a, b\}$, $\{b, c\}$, $\{b, d\}$, and $\{a, b, c, d\}$.

To see why, ... 
\textcolor{orange}{(WIP: Podać punkty i promienie (VCR), następnie opisać dlaczego nie jest CR ani VR ... Obrazek?)}   
\end{example}

% =====================
% Techniques
% =====================

\section{\textcolor{orange}{Methodology/Techniques (WIP: Inny tytuł?)}}\label{{ch2:sec:methods}}

\subsection{Integer Linear Program}
Integer linear programming (ILP) is a common approach to solve NP-hard problems, 
furthermore it can be used successfully for other problems as well.
To solve a problem by reduction to the linear program,
first we need to define the given problem as a system of linear inequalities and an objective function.
Formally, for $j \in [n]$, a single inequality consists of
a set of decision variables $x_j$,
a set of parameters $a_j$ and
a constraint $b$,
together giving a formula: $a_{1}x_{1} + a_{2}x_{2} + ... + a_{n}x_{n} \leq b$
However, in practice there are multiple constraint inequalities, and we use more general notation to represent an i-th inequality:
\[\sum_{j=1}^{n} a_{ij}x_{j} \leq b_{i}\]
Besides constraints, we define an objective function with parameters $c_j$:
\[\sum_{j=1}^{n} c_{j}x_{j}\]
Now, having defined inequalities and the objective, 
the goal is to find a vector of values for the $x_j$,
such that it maximizes (or minimizes) the value of the objective function, while keeping all the inequalities true.
In general, all the variables of a linear program can be fractional, and such program can be solved in polynomial time.
However, there is a special case called \textit{(mixed) integer linear program},
that is, when we require some variables to be integers. Solving integer linear programs is NP-hard.
Despite this, there are available state-of-the-art linear program solvers, that solve ILPs effectively.
In this work we use one of them, the Gurobi solver.
For the complete theory of (integer) linear programming, we point to the book by \cite{ilp-book-wolsey}.

Not every problem can be directly defined as an integer linear program.
However, we can apply basic techniques and tricks to make the problem suitable.
For example, this is the case for our solution to the VCR profile detection problem described in Chapter \ref{ch:detection}.
Below we present these methods:
\newline

\noindent \textbf{Big-M Method.} \indent This technique is used to overcome a variety of problems,
such as representing absolute or minimum/maximum values as linear inequalities.
Another examples would be defining a logical conditions (And/Or) between the inequalities or using indicators (i.e., if-then logic).
These problems are not linear in their nature. However, by taking certain steps we can linearize them,
and then use them in a linear program.


Having a set of $m$ linear inequalities with $n$ variables represented as:
\[\sum_{j=1}^{n} a_{ij}x_{j} \leq b_{i} \qquad i \in [m], j \in [n]\]
The fundamental concept of the BigM method is to introduce to the inequalities,
an additional large constant coefficient $M$ (hence Big), which is greater than the maximum value of any variable $x_j$.
Based on how we do it decides which of the aforementioned problems we solve.
Below in Example \ref{ch2:ex:ilp-bigm-or}, we showcase the BigM technique to represent $OR$ condition between inequalities.

\begin{example}\label{ch2:ex:ilp-bigm-or}
    We have two inequalities and want to represent a logical $OR$ condition between them.
    \[\sum_{j=1}^{n} a_{1j}x_{j} \leq b_{1} \quad or \quad \sum_{j=1}^{n} a_{2j}x_{j} \leq b_{2}.\]
    To do so, we introduce a large constant $M$ and two binary variables $y_1, y_2 \in \{0,1\}$.  First we transform the inequalities:
    \[\sum_{j=1}^{n} a_{1j}x_{j} \leq b_{1} + M(1 - y_1)\]
    \[\sum_{j=1}^{n} a_{2j}x_{j} \leq b_{2} + M(1 - y_2)\]
    Then, we add one more constraint, binding the inequalities together:
    \[y_1 + y_2 \geq 1\]
    This constraint enforces that at least one of the inequalities will be true.
\end{example}

The BigM method is a valuable and widely used tool, however it has some downsides, mostly related to the numerical issues.
That is why, most ILP solvers provide additional built-in mechanisms for applying it.
For further reading, we point to the reference manual of \cite{ilp-gurobi}.
We use one of those mechanisms for our linear program in Chapter \ref{ch:detection}, namely an \textit{indicator constraint}.
It allows one to create constraints representing if-then logic.
Formally, we have to introduce a binary variable $z \in \{0,1\}$, and then we can define an indicator constraint in the form of:
\[z = f \longrightarrow \sum_{j=1}^{n} a_{ij}x_{j} \leq b_{i}\]
The constraint states that if variable $z$ is equal to $f \in \{0,1\}$, then the linear constraint should hold.
On the other hand, if $z = 1 - f$, then the linear consist may be violated.